<h2><%= @reservation.lecture.title %></h2>
 <p><%= @reservation.location %></p>

<%= link_to 'Delete reservation', lecture_reservation_path(@reservation.lecture, @reservation), method: :delete, data: { confirm: 'Are you sure you want to cancel this reservation?' } %>

<%= link_to 'Back to reservations', lecture_reservations_path %>
