  <div class="container mt-5">
<div class="row">
<div class="col">

<div class="card mb-4">
<div class="card-body">
<h2><%= @reservation.lecture.title %></h2>
 <p><%= @reservation.location %></p>

<%= link_to 'Delete reservation', lecture_reservation_path(@reservation.lecture, @reservation), method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to cancel this reservation?' } %>

<%= link_to 'Back to reservations', lecture_reservations_path, class: 'btn btn-primary' %>

</div></div></div>        </div>        </div>
