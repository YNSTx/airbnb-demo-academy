<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <title>New Reservation</title>
</head>
<body>

  <div class="container mt-5">
    <h1>New Reservation</h1>

    <p>Available Places: <%= @lecture.available_places %></p>

    <%= simple_form_for @reservation, url: lecture_reservations_path(@lecture), method: :post do |f| %>
      <div class="reserv_container">
        <%= f.input :location, label: 'Location', class: 'form-control mb-3' %>

        <%= f.submit 'Create Reservation', class: 'btn btn-primary' %>
      </div>
    <% end %>

    <%= link_to 'Back to lectures', lectures_path, class: 'btn btn-secondary mt-3' %>
  </div>

  

</body>
</html>
